[project]
name = "ganache-boost"
authors = ["Ganache Fitness Team"]
description = "A decentralized fitness tracking and challenge platform"
telemetry = false
requirements = []
[contracts.ganache-fitness-tracker]
path = "contracts/ganache-fitness-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_functions"]

[repl.analysis.check_functions]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
